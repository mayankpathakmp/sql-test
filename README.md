# SQL Query Bench

Static HTML quiz for SQL fundamentals. Open `sql-query-bench.html` in a browser to run the quiz.

This quiz enforces a 45-minute timer for a 50-question session drawn from the full 100-question pool.
